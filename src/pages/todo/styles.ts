import { Box, styled } from "@mui/material";

export const TodoRoot = styled(Box)({
	display: "flex",
	alignItems: "center",
	justifyContent: "space-between",
	flexWrap: "wrap",
	gap: 12,
	marginBottom: 12
});
